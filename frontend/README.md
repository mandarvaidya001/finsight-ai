# FinSight Dashboard

React frontend for the FinSight AI analytics API.

## Setup

1. `cd frontend`
2. `npm install`
3. `npm start`

## Notes

- Expects backend at `http://127.0.0.1:8000/api`
- Uses Tailwind CSS and Recharts for charts
